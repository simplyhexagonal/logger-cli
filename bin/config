#!/usr/bin/env bash

if [ "$(uname -s)" = 'Linux' ]; then
  LOGGER_PATH="$(dirname "$(readlink -f "$0")")"
else
  LOGGER_PATH="$(dirname "$(readlink "$0")")"
fi

echo $LOGGER_PATH

rm -f $LOGGER_PATH/../logger.config.js

ln -s $1 $LOGGER_PATH/../logger.config.js
