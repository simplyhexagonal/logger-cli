#!/usr/bin/env bash

LOGGER_PATH="$(dirname "$(node -e "console.log(require('readlink').sync(\"$0\"))")")"

echo $LOGGER_PATH

rm -f $LOGGER_PATH/../logger.config.js

ln -s $1 $LOGGER_PATH/../logger.config.js
